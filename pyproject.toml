[project]
name = "eurotape"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Avenkar", email = "0xavenkar@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
eurotape = "eurotape:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ruff>=0.16.7",
]
